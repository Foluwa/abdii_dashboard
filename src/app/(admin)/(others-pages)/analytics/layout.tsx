"use client";

import React, { useState } from 'react';
import { useGameAnalytics } from '@/hooks/useApi';
import PageBreadCrumb from '@/components/common/PageBreadCrumb';
import Alert from '@/components/ui/alert/SimpleAlert';

export default function GameAnalyticsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
